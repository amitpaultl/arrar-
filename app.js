// 1st
const stop = (fit, lest, jree) => fit + lest + jree;

// 2sn

const mom = `

I am a web developer.
I love to code.
I love to eat biryani.
`
// 3th

const Oppo =  (Cla, rom, ram = 2) => Cla +' '+ rom + ' '+ ram;

// 4th

const array = ['Loop', 'bank', 'goos', 'Ripon'];
let even = []
for (let i = 0; i < array.length; i++) {
    const element = array[i];
    const num = element.length;
    if(num %2 === 0){
        even.push(element)
        
    }
    
}

// 6th

const suer= [10,5, 6,4,12];
let squer = 0
for (let i = 0; i < suer.length; i++) {
    const element = suer[i];
    const su = element *element;
    squer = squer  + su;

}
const loops = suer.length;
const rop = squer / loops;

// 7th

const fist = [12,41,25,0,12,32,11];
const sece = [4,15,085,40,4,0,5,04];
const total = [...fist , ...sece]
const vul = Math.max(...total)


const {a, b, z} = {a:12, b:12, z:'ghy', hello:22}

const user ={
    about:12,
    id:12,
    class:{
        roll:12,
        regi:{
            mium:'jkeo',
            kolp: 12,
            lop:{
                lopi:45,
                lo:'l'
            }
        }
    }
}

const number = [12,20,4,20]

const dobul = x => x

const getdobal = number.map(dobul)

const numbere =  [12,23,41,25,12,10] ;
const  totals = numbere.reduce((p, caerwnt)=> p+ caerwnt,0) 
const student ={
    mane:'rajip',
    roll_12:12

}
const tolatses =student.roll_12



class Instor{
    name;
    disimg = 'web develop';
    team='web tem';
    location;
    constructor(name, locti){
this.name =name;
this.location = locti  ;

    }
}

   
    const arrim = new Instor('Amit', 'panjia');
   
    function min(n){return Math.min(n)}
    console.log(min[1,3,2]);